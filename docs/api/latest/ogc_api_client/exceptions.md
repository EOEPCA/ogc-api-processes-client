# Module ogc_api_client.exceptions

A sample API conforming to the OGC API - Processes - Part 1 standard

This document is an API definition document provided alongside the OGC API - Processes standard. The OGC API - Processes Standard specifies a processing interface to communicate over a RESTful protocol using JavaScript Object Notation (JSON) encodings. The specification allows for the wrapping of computational tasks into executable processes that can be offered by a server and be invoked by a client application.

The version of the OpenAPI document: 1.0.0
Contact: standards-team@ogc.org
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.

## Functions

    
### render_path

```python3
def render_path(
    path_to_item
)
```

Returns a string representation of a path

## Classes

### ApiAttributeError

```python3
class ApiAttributeError(
    msg,
    path_to_item=None
)
```

The base exception class for all OpenAPIExceptions

#### Ancestors (in MRO)

* ogc_api_client.exceptions.OpenApiException
* builtins.AttributeError
* builtins.Exception
* builtins.BaseException

#### Class variables

```python3
args
```

```python3
name
```

```python3
obj
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.

### ApiException

```python3
class ApiException(
    status=None,
    reason=None,
    http_resp=None,
    *,
    body: Optional[str] = None,
    data: Optional[Any] = None
)
```

The base exception class for all OpenAPIExceptions

#### Ancestors (in MRO)

* ogc_api_client.exceptions.OpenApiException
* builtins.Exception
* builtins.BaseException

#### Descendants

* ogc_api_client.exceptions.BadRequestException
* ogc_api_client.exceptions.NotFoundException
* ogc_api_client.exceptions.UnauthorizedException
* ogc_api_client.exceptions.ForbiddenException
* ogc_api_client.exceptions.ServiceException
* ogc_api_client.exceptions.ConflictException
* ogc_api_client.exceptions.UnprocessableEntityException

#### Class variables

```python3
args
```

#### Static methods

    
#### from_response

```python3
def from_response(
    *,
    http_resp,
    body: Optional[str],
    data: Optional[Any]
) -> Self
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.

### ApiKeyError

```python3
class ApiKeyError(
    msg,
    path_to_item=None
)
```

The base exception class for all OpenAPIExceptions

#### Ancestors (in MRO)

* ogc_api_client.exceptions.OpenApiException
* builtins.KeyError
* builtins.LookupError
* builtins.Exception
* builtins.BaseException

#### Class variables

```python3
args
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.

### ApiTypeError

```python3
class ApiTypeError(
    msg,
    path_to_item=None,
    valid_classes=None,
    key_type=None
)
```

The base exception class for all OpenAPIExceptions

#### Ancestors (in MRO)

* ogc_api_client.exceptions.OpenApiException
* builtins.TypeError
* builtins.Exception
* builtins.BaseException

#### Class variables

```python3
args
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.

### ApiValueError

```python3
class ApiValueError(
    msg,
    path_to_item=None
)
```

The base exception class for all OpenAPIExceptions

#### Ancestors (in MRO)

* ogc_api_client.exceptions.OpenApiException
* builtins.ValueError
* builtins.Exception
* builtins.BaseException

#### Class variables

```python3
args
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.

### BadRequestException

```python3
class BadRequestException(
    status=None,
    reason=None,
    http_resp=None,
    *,
    body: Optional[str] = None,
    data: Optional[Any] = None
)
```

The base exception class for all OpenAPIExceptions

#### Ancestors (in MRO)

* ogc_api_client.exceptions.ApiException
* ogc_api_client.exceptions.OpenApiException
* builtins.Exception
* builtins.BaseException

#### Class variables

```python3
args
```

#### Static methods

    
#### from_response

```python3
def from_response(
    *,
    http_resp,
    body: Optional[str],
    data: Optional[Any]
) -> Self
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.

### ConflictException

```python3
class ConflictException(
    status=None,
    reason=None,
    http_resp=None,
    *,
    body: Optional[str] = None,
    data: Optional[Any] = None
)
```

Exception for HTTP 409 Conflict.

#### Ancestors (in MRO)

* ogc_api_client.exceptions.ApiException
* ogc_api_client.exceptions.OpenApiException
* builtins.Exception
* builtins.BaseException

#### Class variables

```python3
args
```

#### Static methods

    
#### from_response

```python3
def from_response(
    *,
    http_resp,
    body: Optional[str],
    data: Optional[Any]
) -> Self
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.

### ForbiddenException

```python3
class ForbiddenException(
    status=None,
    reason=None,
    http_resp=None,
    *,
    body: Optional[str] = None,
    data: Optional[Any] = None
)
```

The base exception class for all OpenAPIExceptions

#### Ancestors (in MRO)

* ogc_api_client.exceptions.ApiException
* ogc_api_client.exceptions.OpenApiException
* builtins.Exception
* builtins.BaseException

#### Class variables

```python3
args
```

#### Static methods

    
#### from_response

```python3
def from_response(
    *,
    http_resp,
    body: Optional[str],
    data: Optional[Any]
) -> Self
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.

### NotFoundException

```python3
class NotFoundException(
    status=None,
    reason=None,
    http_resp=None,
    *,
    body: Optional[str] = None,
    data: Optional[Any] = None
)
```

The base exception class for all OpenAPIExceptions

#### Ancestors (in MRO)

* ogc_api_client.exceptions.ApiException
* ogc_api_client.exceptions.OpenApiException
* builtins.Exception
* builtins.BaseException

#### Class variables

```python3
args
```

#### Static methods

    
#### from_response

```python3
def from_response(
    *,
    http_resp,
    body: Optional[str],
    data: Optional[Any]
) -> Self
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.

### OpenApiException

```python3
class OpenApiException(
    /,
    *args,
    **kwargs
)
```

The base exception class for all OpenAPIExceptions

#### Ancestors (in MRO)

* builtins.Exception
* builtins.BaseException

#### Descendants

* ogc_api_client.exceptions.ApiTypeError
* ogc_api_client.exceptions.ApiValueError
* ogc_api_client.exceptions.ApiAttributeError
* ogc_api_client.exceptions.ApiKeyError
* ogc_api_client.exceptions.ApiException

#### Class variables

```python3
args
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.

### ServiceException

```python3
class ServiceException(
    status=None,
    reason=None,
    http_resp=None,
    *,
    body: Optional[str] = None,
    data: Optional[Any] = None
)
```

The base exception class for all OpenAPIExceptions

#### Ancestors (in MRO)

* ogc_api_client.exceptions.ApiException
* ogc_api_client.exceptions.OpenApiException
* builtins.Exception
* builtins.BaseException

#### Class variables

```python3
args
```

#### Static methods

    
#### from_response

```python3
def from_response(
    *,
    http_resp,
    body: Optional[str],
    data: Optional[Any]
) -> Self
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.

### UnauthorizedException

```python3
class UnauthorizedException(
    status=None,
    reason=None,
    http_resp=None,
    *,
    body: Optional[str] = None,
    data: Optional[Any] = None
)
```

The base exception class for all OpenAPIExceptions

#### Ancestors (in MRO)

* ogc_api_client.exceptions.ApiException
* ogc_api_client.exceptions.OpenApiException
* builtins.Exception
* builtins.BaseException

#### Class variables

```python3
args
```

#### Static methods

    
#### from_response

```python3
def from_response(
    *,
    http_resp,
    body: Optional[str],
    data: Optional[Any]
) -> Self
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.

### UnprocessableEntityException

```python3
class UnprocessableEntityException(
    status=None,
    reason=None,
    http_resp=None,
    *,
    body: Optional[str] = None,
    data: Optional[Any] = None
)
```

Exception for HTTP 422 Unprocessable Entity.

#### Ancestors (in MRO)

* ogc_api_client.exceptions.ApiException
* ogc_api_client.exceptions.OpenApiException
* builtins.Exception
* builtins.BaseException

#### Class variables

```python3
args
```

#### Static methods

    
#### from_response

```python3
def from_response(
    *,
    http_resp,
    body: Optional[str],
    data: Optional[Any]
) -> Self
```

#### Methods

    
#### add_note

```python3
def add_note(
    self,
    object,
    /
)
```

Exception.add_note(note) --

add a note to the exception

    
#### with_traceback

```python3
def with_traceback(
    self,
    object,
    /
)
```

Exception.with_traceback(tb) --

set self.__traceback__ to tb and return self.